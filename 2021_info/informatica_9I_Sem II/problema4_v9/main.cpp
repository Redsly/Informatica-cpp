#include <iostream>
#include <fstream>
using namespace std;

int main()
{
    ifstream in("bac.txt");
    int a[20],n=0,k=1000, nr_a=0;
    while(!in.eof()){
        cin>>a[n];
        n++;
    }

    for(int k=1000;k>0;k--){
        for(int i=0;i<n;i++){
            if(a[i])
        }
    }
    return 0;
}
